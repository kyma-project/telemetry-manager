# Populate Images

Small tool for generating the security scanner config file out of the images configured in the .env file.

## Build locally

To build the app locally run:

```sh
go build -o populate-images main.go
```
