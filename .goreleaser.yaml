# Documentation for how to configure goreleaser can be found at http://goreleaser.com
changelog:
  use: github
  sort: asc
release:
  extra_files:
    - glob: template.yaml
    - glob: ./charts/telemetry/templates/rendered.yaml
builds:
  -
    skip: true
