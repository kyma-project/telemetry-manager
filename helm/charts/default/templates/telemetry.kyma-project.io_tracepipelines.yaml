---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  labels:
    app.kubernetes.io/component: controller
    app.kubernetes.io/managed-by: kyma
    app.kubernetes.io/name: telemetry-manager
    app.kubernetes.io/part-of: telemetry
    kyma-project.io/module: telemetry
  name: tracepipelines.telemetry.kyma-project.io
spec:
  group: telemetry.kyma-project.io
  names:
    categories:
    - kyma-telemetry
    - kyma-telemetry-pipelines
    kind: TracePipeline
    listKind: TracePipelineList
    plural: tracepipelines
    singular: tracepipeline
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="ConfigurationGenerated")].status
      name: Configuration Generated
      type: string
    - jsonPath: .status.conditions[?(@.type=="GatewayHealthy")].status
      name: Gateway Healthy
      type: string
    - jsonPath: .status.conditions[?(@.type=="TelemetryFlowHealthy")].status
      name: Flow Healthy
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TracePipeline is the Schema for the tracepipelines API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of TracePipeline
            properties:
              filter:
                description: Filter specifies a list of filters to apply to telemetry
                  data.
                items:
                  description: FilterSpec defines a filter to apply to telemetry data.
                  properties:
                    conditions:
                      description: Conditions specify a list of multiple conditions
                        which are ORed together, which means only one condition needs
                        to evaluate to true in order for the telemetry to be dropped.
                      items:
                        type: string
                      type: array
                  type: object
                type: array
              output:
                description: Output configures the backend to which traces are sent.
                  You must specify exactly one output per pipeline.
                properties:
                  otlp:
                    description: OTLP output defines an output using the OpenTelemetry
                      protocol.
                    properties:
                      authentication:
                        description: Authentication defines authentication options
                          for the OTLP output
                        properties:
                          basic:
                            description: Basic activates `Basic` authentication for
                              the destination providing relevant Secrets.
                            properties:
                              password:
                                description: Password contains the basic auth password
                                  or a Secret reference.
                                properties:
                                  value:
                                    description: Value as plain text.
                                    type: string
                                  valueFrom:
                                    description: ValueFrom is the value as a reference
                                      to a resource.
                                    properties:
                                      secretKeyRef:
                                        description: SecretKeyRef refers to the value
                                          of a specific key in a Secret. You must
                                          provide `name` and `namespace` of the Secret,
                                          as well as the name of the `key`.
                                        properties:
                                          key:
                                            description: Key defines the name of the
                                              attribute of the Secret holding the
                                              referenced value.
                                            minLength: 1
                                            type: string
                                          name:
                                            description: Name of the Secret containing
                                              the referenced value.
                                            minLength: 1
                                            type: string
                                          namespace:
                                            description: Namespace containing the
                                              Secret with the referenced value.
                                            minLength: 1
                                            type: string
                                        required:
                                        - key
                                        - name
                                        - namespace
                                        type: object
                                    required:
                                    - secretKeyRef
                                    type: object
                                type: object
                                x-kubernetes-validations:
                                - message: Only one of 'value' or 'valueFrom' can
                                    be set
                                  rule: '!(has(self.value) && size(self.value) > 0
                                    && has(self.valueFrom))'
                              user:
                                description: User contains the basic auth username
                                  or a Secret reference.
                                properties:
                                  value:
                                    description: Value as plain text.
                                    type: string
                                  valueFrom:
                                    description: ValueFrom is the value as a reference
                                      to a resource.
                                    properties:
                                      secretKeyRef:
                                        description: SecretKeyRef refers to the value
                                          of a specific key in a Secret. You must
                                          provide `name` and `namespace` of the Secret,
                                          as well as the name of the `key`.
                                        properties:
                                          key:
                                            description: Key defines the name of the
                                              attribute of the Secret holding the
                                              referenced value.
                                            minLength: 1
                                            type: string
                                          name:
                                            description: Name of the Secret containing
                                              the referenced value.
                                            minLength: 1
                                            type: string
                                          namespace:
                                            description: Namespace containing the
                                              Secret with the referenced value.
                                            minLength: 1
                                            type: string
                                        required:
                                        - key
                                        - name
                                        - namespace
                                        type: object
                                    required:
                                    - secretKeyRef
                                    type: object
                                type: object
                                x-kubernetes-validations:
                                - message: Only one of 'value' or 'valueFrom' can
                                    be set
                                  rule: '!(has(self.value) && size(self.value) > 0
                                    && has(self.valueFrom))'
                            required:
                            - password
                            - user
                            type: object
                          oauth2:
                            description: OAuth2 activates `OAuth2` authentication
                              for the destination providing relevant Secrets.
                            properties:
                              clientID:
                                description: ClientID contains the OAuth2 client ID
                                  or a Secret reference.
                                properties:
                                  value:
                                    description: Value as plain text.
                                    type: string
                                  valueFrom:
                                    description: ValueFrom is the value as a reference
                                      to a resource.
                                    properties:
                                      secretKeyRef:
                                        description: SecretKeyRef refers to the value
                                          of a specific key in a Secret. You must
                                          provide `name` and `namespace` of the Secret,
                                          as well as the name of the `key`.
                                        properties:
                                          key:
                                            description: Key defines the name of the
                                              attribute of the Secret holding the
                                              referenced value.
                                            minLength: 1
                                            type: string
                                          name:
                                            description: Name of the Secret containing
                                              the referenced value.
                                            minLength: 1
                                            type: string
                                          namespace:
                                            description: Namespace containing the
                                              Secret with the referenced value.
                                            minLength: 1
                                            type: string
                                        required:
                                        - key
                                        - name
                                        - namespace
                                        type: object
                                    required:
                                    - secretKeyRef
                                    type: object
                                type: object
                                x-kubernetes-validations:
                                - message: '''clientID'' missing'
                                  rule: (self.value != '' ) || (has(self.valueFrom))
                                - message: Only one of 'value' or 'valueFrom' can
                                    be set
                                  rule: '!(has(self.value) && size(self.value) > 0
                                    && has(self.valueFrom))'
                              clientSecret:
                                description: ClientSecret contains the OAuth2 client
                                  secret or a Secret reference.
                                properties:
                                  value:
                                    description: Value as plain text.
                                    type: string
                                  valueFrom:
                                    description: ValueFrom is the value as a reference
                                      to a resource.
                                    properties:
                                      secretKeyRef:
                                        description: SecretKeyRef refers to the value
                                          of a specific key in a Secret. You must
                                          provide `name` and `namespace` of the Secret,
                                          as well as the name of the `key`.
                                        properties:
                                          key:
                                            description: Key defines the name of the
                                              attribute of the Secret holding the
                                              referenced value.
                                            minLength: 1
                                            type: string
                                          name:
                                            description: Name of the Secret containing
                                              the referenced value.
                                            minLength: 1
                                            type: string
                                          namespace:
                                            description: Namespace containing the
                                              Secret with the referenced value.
                                            minLength: 1
                                            type: string
                                        required:
                                        - key
                                        - name
                                        - namespace
                                        type: object
                                    required:
                                    - secretKeyRef
                                    type: object
                                type: object
                                x-kubernetes-validations:
                                - message: clientSecret' missing
                                  rule: (self.value != '' ) || (has(self.valueFrom))
                                - message: Only one of 'value' or 'valueFrom' can
                                    be set
                                  rule: '!(has(self.value) && size(self.value) > 0
                                    && has(self.valueFrom))'
                              params:
                                additionalProperties:
                                  type: string
                                description: Params contains optional additional OAuth2
                                  parameters that are sent to the token endpoint.
                                type: object
                              scopes:
                                description: Scopes contains optional OAuth2 scopes.
                                items:
                                  type: string
                                type: array
                              tokenURL:
                                description: TokenURL contains the OAuth2 token endpoint
                                  URL or a Secret reference.
                                properties:
                                  value:
                                    description: Value as plain text.
                                    type: string
                                  valueFrom:
                                    description: ValueFrom is the value as a reference
                                      to a resource.
                                    properties:
                                      secretKeyRef:
                                        description: SecretKeyRef refers to the value
                                          of a specific key in a Secret. You must
                                          provide `name` and `namespace` of the Secret,
                                          as well as the name of the `key`.
                                        properties:
                                          key:
                                            description: Key defines the name of the
                                              attribute of the Secret holding the
                                              referenced value.
                                            minLength: 1
                                            type: string
                                          name:
                                            description: Name of the Secret containing
                                              the referenced value.
                                            minLength: 1
                                            type: string
                                          namespace:
                                            description: Namespace containing the
                                              Secret with the referenced value.
                                            minLength: 1
                                            type: string
                                        required:
                                        - key
                                        - name
                                        - namespace
                                        type: object
                                    required:
                                    - secretKeyRef
                                    type: object
                                type: object
                                x-kubernetes-validations:
                                - message: tokenURL' missing
                                  rule: (self.value != '' ) || (has(self.valueFrom))
                                - message: '''tokenURL'' must be a valid URL'
                                  rule: '(self.value != '''' ) ? (isURL(self.value))
                                    : true'
                                - message: Only one of 'value' or 'valueFrom' can
                                    be set
                                  rule: '!(has(self.value) && size(self.value) > 0
                                    && has(self.valueFrom))'
                            required:
                            - clientID
                            - clientSecret
                            - tokenURL
                            type: object
                        type: object
                        x-kubernetes-validations:
                        - message: Only one authentication method can be specified
                          rule: '!(has(self.basic) && has(self.oauth2))'
                      endpoint:
                        description: Endpoint defines the host and port (`<host>:<port>`)
                          of an OTLP endpoint.
                        properties:
                          value:
                            description: Value as plain text.
                            type: string
                          valueFrom:
                            description: ValueFrom is the value as a reference to
                              a resource.
                            properties:
                              secretKeyRef:
                                description: SecretKeyRef refers to the value of a
                                  specific key in a Secret. You must provide `name`
                                  and `namespace` of the Secret, as well as the name
                                  of the `key`.
                                properties:
                                  key:
                                    description: Key defines the name of the attribute
                                      of the Secret holding the referenced value.
                                    minLength: 1
                                    type: string
                                  name:
                                    description: Name of the Secret containing the
                                      referenced value.
                                    minLength: 1
                                    type: string
                                  namespace:
                                    description: Namespace containing the Secret with
                                      the referenced value.
                                    minLength: 1
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            required:
                            - secretKeyRef
                            type: object
                        type: object
                        x-kubernetes-validations:
                        - message: Only one of 'value' or 'valueFrom' can be set
                          rule: '!(has(self.value) && size(self.value) > 0 && has(self.valueFrom))'
                      headers:
                        description: Headers defines custom headers to be added to
                          outgoing HTTP or gRPC requests.
                        items:
                          properties:
                            name:
                              description: Name defines the header name.
                              minLength: 1
                              type: string
                            prefix:
                              description: Prefix defines an optional header value
                                prefix. The prefix is separated from the value by
                                a space character.
                              type: string
                            value:
                              description: Value as plain text.
                              type: string
                            valueFrom:
                              description: ValueFrom is the value as a reference to
                                a resource.
                              properties:
                                secretKeyRef:
                                  description: SecretKeyRef refers to the value of
                                    a specific key in a Secret. You must provide `name`
                                    and `namespace` of the Secret, as well as the
                                    name of the `key`.
                                  properties:
                                    key:
                                      description: Key defines the name of the attribute
                                        of the Secret holding the referenced value.
                                      minLength: 1
                                      type: string
                                    name:
                                      description: Name of the Secret containing the
                                        referenced value.
                                      minLength: 1
                                      type: string
                                    namespace:
                                      description: Namespace containing the Secret
                                        with the referenced value.
                                      minLength: 1
                                      type: string
                                  required:
                                  - key
                                  - name
                                  - namespace
                                  type: object
                              required:
                              - secretKeyRef
                              type: object
                          required:
                          - name
                          type: object
                          x-kubernetes-validations:
                          - message: Only one of 'value' or 'valueFrom' can be set
                            rule: '!(has(self.value) && size(self.value) > 0 && has(self.valueFrom))'
                        type: array
                      path:
                        description: Path defines OTLP export URL path (only for the
                          HTTP protocol). This value overrides auto-appended paths
                          `/v1/logs`, `/v1/metrics`, and `/v1/traces`
                        type: string
                      protocol:
                        description: Protocol defines the OTLP protocol (`http` or
                          `grpc`). Default is `grpc`.
                        enum:
                        - grpc
                        - http
                        type: string
                      tls:
                        description: TLS defines TLS options for the OTLP output.
                        properties:
                          ca:
                            description: Defines an optional CA certificate for server
                              certificate verification when using TLS. The certificate
                              must be provided in PEM format.
                            properties:
                              value:
                                description: Value as plain text.
                                type: string
                              valueFrom:
                                description: ValueFrom is the value as a reference
                                  to a resource.
                                properties:
                                  secretKeyRef:
                                    description: SecretKeyRef refers to the value
                                      of a specific key in a Secret. You must provide
                                      `name` and `namespace` of the Secret, as well
                                      as the name of the `key`.
                                    properties:
                                      key:
                                        description: Key defines the name of the attribute
                                          of the Secret holding the referenced value.
                                        minLength: 1
                                        type: string
                                      name:
                                        description: Name of the Secret containing
                                          the referenced value.
                                        minLength: 1
                                        type: string
                                      namespace:
                                        description: Namespace containing the Secret
                                          with the referenced value.
                                        minLength: 1
                                        type: string
                                    required:
                                    - key
                                    - name
                                    - namespace
                                    type: object
                                required:
                                - secretKeyRef
                                type: object
                            type: object
                            x-kubernetes-validations:
                            - message: Only one of 'value' or 'valueFrom' can be set
                              rule: '!(has(self.value) && size(self.value) > 0 &&
                                has(self.valueFrom))'
                          cert:
                            description: Defines a client certificate to use when
                              using TLS. The certificate must be provided in PEM format.
                            properties:
                              value:
                                description: Value as plain text.
                                type: string
                              valueFrom:
                                description: ValueFrom is the value as a reference
                                  to a resource.
                                properties:
                                  secretKeyRef:
                                    description: SecretKeyRef refers to the value
                                      of a specific key in a Secret. You must provide
                                      `name` and `namespace` of the Secret, as well
                                      as the name of the `key`.
                                    properties:
                                      key:
                                        description: Key defines the name of the attribute
                                          of the Secret holding the referenced value.
                                        minLength: 1
                                        type: string
                                      name:
                                        description: Name of the Secret containing
                                          the referenced value.
                                        minLength: 1
                                        type: string
                                      namespace:
                                        description: Namespace containing the Secret
                                          with the referenced value.
                                        minLength: 1
                                        type: string
                                    required:
                                    - key
                                    - name
                                    - namespace
                                    type: object
                                required:
                                - secretKeyRef
                                type: object
                            type: object
                            x-kubernetes-validations:
                            - message: Only one of 'value' or 'valueFrom' can be set
                              rule: '!(has(self.value) && size(self.value) > 0 &&
                                has(self.valueFrom))'
                          insecure:
                            description: Insecure defines whether to send requests
                              using plaintext instead of TLS.
                            type: boolean
                          insecureSkipVerify:
                            description: InsecureSkipVerify defines whether to skip
                              server certificate verification when using TLS.
                            type: boolean
                          key:
                            description: Defines the client key to use when using
                              TLS. The key must be provided in PEM format.
                            properties:
                              value:
                                description: Value as plain text.
                                type: string
                              valueFrom:
                                description: ValueFrom is the value as a reference
                                  to a resource.
                                properties:
                                  secretKeyRef:
                                    description: SecretKeyRef refers to the value
                                      of a specific key in a Secret. You must provide
                                      `name` and `namespace` of the Secret, as well
                                      as the name of the `key`.
                                    properties:
                                      key:
                                        description: Key defines the name of the attribute
                                          of the Secret holding the referenced value.
                                        minLength: 1
                                        type: string
                                      name:
                                        description: Name of the Secret containing
                                          the referenced value.
                                        minLength: 1
                                        type: string
                                      namespace:
                                        description: Namespace containing the Secret
                                          with the referenced value.
                                        minLength: 1
                                        type: string
                                    required:
                                    - key
                                    - name
                                    - namespace
                                    type: object
                                required:
                                - secretKeyRef
                                type: object
                            type: object
                            x-kubernetes-validations:
                            - message: Only one of 'value' or 'valueFrom' can be set
                              rule: '!(has(self.value) && size(self.value) > 0 &&
                                has(self.valueFrom))'
                        type: object
                        x-kubernetes-validations:
                        - message: Can define either both 'cert' and 'key', or neither
                          rule: has(self.cert) == has(self.key)
                    required:
                    - endpoint
                    type: object
                    x-kubernetes-validations:
                    - message: Path is only available with HTTP protocol
                      rule: '(has(self.path) && size(self.path) > 0) ? self.protocol
                        == ''http'' : true'
                    - message: OAuth2 authentication requires TLS when using gRPC
                        protocol
                      rule: '(has(self.authentication) && has(self.authentication.oauth2)
                        && self.protocol == ''grpc'' && has(self.tls)) ? !(has(self.tls.insecure)
                        && self.tls.insecure == true) : true'
                required:
                - otlp
                type: object
              transform:
                description: Transforms specify a list of transformations to apply
                  to telemetry data.
                items:
                  description: TransformSpec defines a transformation to apply to
                    telemetry data.
                  properties:
                    conditions:
                      description: Conditions specify a list of multiple where clauses,
                        which will be processed as global conditions for the accompanying
                        set of statements. The conditions are ORed together, which
                        means only one condition needs to evaluate to true in order
                        for the statements (including their individual where clauses)
                        to be executed.
                      items:
                        type: string
                      type: array
                    statements:
                      description: Statements specify a list of OTTL statements to
                        perform the transformation.
                      items:
                        type: string
                      type: array
                  type: object
                type: array
            required:
            - output
            type: object
          status:
            description: Status shows the observed state of the TracePipeline
            properties:
              conditions:
                description: An array of conditions describing the status of the pipeline.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="ConfigurationGenerated")].status
      name: Configuration Generated
      type: string
    - jsonPath: .status.conditions[?(@.type=="GatewayHealthy")].status
      name: Gateway Healthy
      type: string
    - jsonPath: .status.conditions[?(@.type=="TelemetryFlowHealthy")].status
      name: Flow Healthy
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: TracePipeline is the Schema for the tracepipelines API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of TracePipeline
            properties:
              filter:
                description: Filter specifies a list of filters to apply to telemetry
                  data.
                items:
                  description: FilterSpec defines a filter to apply to telemetry data.
                  properties:
                    conditions:
                      description: Conditions specify a list of multiple conditions
                        which are ORed together, which means only one condition needs
                        to evaluate to true in order for the telemetry to be dropped.
                      items:
                        type: string
                      type: array
                  type: object
                type: array
              output:
                description: Output configures the backend to which traces are sent.
                  You must specify exactly one output per pipeline.
                properties:
                  otlp:
                    description: OTLP output defines an output using the OpenTelemetry
                      protocol.
                    properties:
                      authentication:
                        description: Authentication defines authentication options
                          for the OTLP output
                        properties:
                          basic:
                            description: Basic activates `Basic` authentication for
                              the destination providing relevant Secrets.
                            properties:
                              password:
                                description: Password contains the basic auth password
                                  or a Secret reference.
                                properties:
                                  value:
                                    description: Value as plain text.
                                    type: string
                                  valueFrom:
                                    description: ValueFrom is the value as a reference
                                      to a resource.
                                    properties:
                                      secretKeyRef:
                                        description: SecretKeyRef refers to the value
                                          of a specific key in a Secret. You must
                                          provide `name` and `namespace` of the Secret,
                                          as well as the name of the `key`.
                                        properties:
                                          key:
                                            description: Key defines the name of the
                                              attribute of the Secret holding the
                                              referenced value.
                                            minLength: 1
                                            type: string
                                          name:
                                            description: Name of the Secret containing
                                              the referenced value.
                                            minLength: 1
                                            type: string
                                          namespace:
                                            description: Namespace containing the
                                              Secret with the referenced value.
                                            minLength: 1
                                            type: string
                                        required:
                                        - key
                                        - name
                                        - namespace
                                        type: object
                                    required:
                                    - secretKeyRef
                                    type: object
                                type: object
                                x-kubernetes-validations:
                                - message: Exactly one of 'value' or 'valueFrom' must
                                    be set
                                  rule: has(self.value) != has(self.valueFrom)
                              user:
                                description: User contains the basic auth username
                                  or a Secret reference.
                                properties:
                                  value:
                                    description: Value as plain text.
                                    type: string
                                  valueFrom:
                                    description: ValueFrom is the value as a reference
                                      to a resource.
                                    properties:
                                      secretKeyRef:
                                        description: SecretKeyRef refers to the value
                                          of a specific key in a Secret. You must
                                          provide `name` and `namespace` of the Secret,
                                          as well as the name of the `key`.
                                        properties:
                                          key:
                                            description: Key defines the name of the
                                              attribute of the Secret holding the
                                              referenced value.
                                            minLength: 1
                                            type: string
                                          name:
                                            description: Name of the Secret containing
                                              the referenced value.
                                            minLength: 1
                                            type: string
                                          namespace:
                                            description: Namespace containing the
                                              Secret with the referenced value.
                                            minLength: 1
                                            type: string
                                        required:
                                        - key
                                        - name
                                        - namespace
                                        type: object
                                    required:
                                    - secretKeyRef
                                    type: object
                                type: object
                                x-kubernetes-validations:
                                - message: Exactly one of 'value' or 'valueFrom' must
                                    be set
                                  rule: has(self.value) != has(self.valueFrom)
                            required:
                            - password
                            - user
                            type: object
                          oauth2:
                            description: OAuth2 activates `OAuth2` authentication
                              for the destination providing relevant Secrets.
                            properties:
                              clientID:
                                description: ClientID contains the OAuth2 client ID
                                  or a Secret reference.
                                properties:
                                  value:
                                    description: Value as plain text.
                                    type: string
                                  valueFrom:
                                    description: ValueFrom is the value as a reference
                                      to a resource.
                                    properties:
                                      secretKeyRef:
                                        description: SecretKeyRef refers to the value
                                          of a specific key in a Secret. You must
                                          provide `name` and `namespace` of the Secret,
                                          as well as the name of the `key`.
                                        properties:
                                          key:
                                            description: Key defines the name of the
                                              attribute of the Secret holding the
                                              referenced value.
                                            minLength: 1
                                            type: string
                                          name:
                                            description: Name of the Secret containing
                                              the referenced value.
                                            minLength: 1
                                            type: string
                                          namespace:
                                            description: Namespace containing the
                                              Secret with the referenced value.
                                            minLength: 1
                                            type: string
                                        required:
                                        - key
                                        - name
                                        - namespace
                                        type: object
                                    required:
                                    - secretKeyRef
                                    type: object
                                type: object
                                x-kubernetes-validations:
                                - message: Exactly one of 'value' or 'valueFrom' must
                                    be set
                                  rule: has(self.value) != has(self.valueFrom)
                              clientSecret:
                                description: ClientSecret contains the OAuth2 client
                                  secret or a Secret reference.
                                properties:
                                  value:
                                    description: Value as plain text.
                                    type: string
                                  valueFrom:
                                    description: ValueFrom is the value as a reference
                                      to a resource.
                                    properties:
                                      secretKeyRef:
                                        description: SecretKeyRef refers to the value
                                          of a specific key in a Secret. You must
                                          provide `name` and `namespace` of the Secret,
                                          as well as the name of the `key`.
                                        properties:
                                          key:
                                            description: Key defines the name of the
                                              attribute of the Secret holding the
                                              referenced value.
                                            minLength: 1
                                            type: string
                                          name:
                                            description: Name of the Secret containing
                                              the referenced value.
                                            minLength: 1
                                            type: string
                                          namespace:
                                            description: Namespace containing the
                                              Secret with the referenced value.
                                            minLength: 1
                                            type: string
                                        required:
                                        - key
                                        - name
                                        - namespace
                                        type: object
                                    required:
                                    - secretKeyRef
                                    type: object
                                type: object
                                x-kubernetes-validations:
                                - message: Exactly one of 'value' or 'valueFrom' must
                                    be set
                                  rule: has(self.value) != has(self.valueFrom)
                              params:
                                additionalProperties:
                                  type: string
                                description: Params contains optional additional OAuth2
                                  parameters that are sent to the token endpoint.
                                type: object
                              scopes:
                                description: Scopes contains optional OAuth2 scopes.
                                items:
                                  type: string
                                type: array
                              tokenURL:
                                description: TokenURL contains the OAuth2 token endpoint
                                  URL or a Secret reference.
                                properties:
                                  value:
                                    description: Value as plain text.
                                    type: string
                                  valueFrom:
                                    description: ValueFrom is the value as a reference
                                      to a resource.
                                    properties:
                                      secretKeyRef:
                                        description: SecretKeyRef refers to the value
                                          of a specific key in a Secret. You must
                                          provide `name` and `namespace` of the Secret,
                                          as well as the name of the `key`.
                                        properties:
                                          key:
                                            description: Key defines the name of the
                                              attribute of the Secret holding the
                                              referenced value.
                                            minLength: 1
                                            type: string
                                          name:
                                            description: Name of the Secret containing
                                              the referenced value.
                                            minLength: 1
                                            type: string
                                          namespace:
                                            description: Namespace containing the
                                              Secret with the referenced value.
                                            minLength: 1
                                            type: string
                                        required:
                                        - key
                                        - name
                                        - namespace
                                        type: object
                                    required:
                                    - secretKeyRef
                                    type: object
                                type: object
                                x-kubernetes-validations:
                                - message: '''tokenURL'' must be a valid URL'
                                  rule: '(self.value != '''' ) ? (isURL(self.value))
                                    : true'
                                - message: Exactly one of 'value' or 'valueFrom' must
                                    be set
                                  rule: has(self.value) != has(self.valueFrom)
                            required:
                            - clientID
                            - clientSecret
                            - tokenURL
                            type: object
                        type: object
                        x-kubernetes-validations:
                        - message: Only one authentication method can be specified
                          rule: '!(has(self.basic) && has(self.oauth2))'
                      endpoint:
                        description: Endpoint defines the host and port (`<host>:<port>`)
                          of an OTLP endpoint.
                        properties:
                          value:
                            description: Value as plain text.
                            type: string
                          valueFrom:
                            description: ValueFrom is the value as a reference to
                              a resource.
                            properties:
                              secretKeyRef:
                                description: SecretKeyRef refers to the value of a
                                  specific key in a Secret. You must provide `name`
                                  and `namespace` of the Secret, as well as the name
                                  of the `key`.
                                properties:
                                  key:
                                    description: Key defines the name of the attribute
                                      of the Secret holding the referenced value.
                                    minLength: 1
                                    type: string
                                  name:
                                    description: Name of the Secret containing the
                                      referenced value.
                                    minLength: 1
                                    type: string
                                  namespace:
                                    description: Namespace containing the Secret with
                                      the referenced value.
                                    minLength: 1
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            required:
                            - secretKeyRef
                            type: object
                        type: object
                        x-kubernetes-validations:
                        - message: Exactly one of 'value' or 'valueFrom' must be set
                          rule: has(self.value) != has(self.valueFrom)
                      headers:
                        description: Headers defines custom headers to be added to
                          outgoing HTTP or gRPC requests.
                        items:
                          properties:
                            name:
                              description: Name defines the header name.
                              minLength: 1
                              type: string
                            prefix:
                              description: Prefix defines an optional header value
                                prefix. The prefix is separated from the value by
                                a space character.
                              type: string
                            value:
                              description: Value as plain text.
                              type: string
                            valueFrom:
                              description: ValueFrom is the value as a reference to
                                a resource.
                              properties:
                                secretKeyRef:
                                  description: SecretKeyRef refers to the value of
                                    a specific key in a Secret. You must provide `name`
                                    and `namespace` of the Secret, as well as the
                                    name of the `key`.
                                  properties:
                                    key:
                                      description: Key defines the name of the attribute
                                        of the Secret holding the referenced value.
                                      minLength: 1
                                      type: string
                                    name:
                                      description: Name of the Secret containing the
                                        referenced value.
                                      minLength: 1
                                      type: string
                                    namespace:
                                      description: Namespace containing the Secret
                                        with the referenced value.
                                      minLength: 1
                                      type: string
                                  required:
                                  - key
                                  - name
                                  - namespace
                                  type: object
                              required:
                              - secretKeyRef
                              type: object
                          required:
                          - name
                          type: object
                          x-kubernetes-validations:
                          - message: Exactly one of 'value' or 'valueFrom' must be
                              set
                            rule: has(self.value) != has(self.valueFrom)
                        type: array
                      path:
                        description: Path defines OTLP export URL path (only for the
                          HTTP protocol). This value overrides auto-appended paths
                          `/v1/logs`, `/v1/metrics`, and `/v1/traces`
                        type: string
                      protocol:
                        description: Protocol defines the OTLP protocol (`http` or
                          `grpc`). Default is `grpc`.
                        enum:
                        - grpc
                        - http
                        type: string
                      tls:
                        description: TLS defines TLS options for the OTLP output.
                        properties:
                          ca:
                            description: Defines an optional CA certificate for server
                              certificate verification when using TLS. The certificate
                              must be provided in PEM format.
                            properties:
                              value:
                                description: Value as plain text.
                                type: string
                              valueFrom:
                                description: ValueFrom is the value as a reference
                                  to a resource.
                                properties:
                                  secretKeyRef:
                                    description: SecretKeyRef refers to the value
                                      of a specific key in a Secret. You must provide
                                      `name` and `namespace` of the Secret, as well
                                      as the name of the `key`.
                                    properties:
                                      key:
                                        description: Key defines the name of the attribute
                                          of the Secret holding the referenced value.
                                        minLength: 1
                                        type: string
                                      name:
                                        description: Name of the Secret containing
                                          the referenced value.
                                        minLength: 1
                                        type: string
                                      namespace:
                                        description: Namespace containing the Secret
                                          with the referenced value.
                                        minLength: 1
                                        type: string
                                    required:
                                    - key
                                    - name
                                    - namespace
                                    type: object
                                required:
                                - secretKeyRef
                                type: object
                            type: object
                            x-kubernetes-validations:
                            - message: Exactly one of 'value' or 'valueFrom' must
                                be set
                              rule: has(self.value) != has(self.valueFrom)
                          cert:
                            description: Defines a client certificate to use when
                              using TLS. The certificate must be provided in PEM format.
                            properties:
                              value:
                                description: Value as plain text.
                                type: string
                              valueFrom:
                                description: ValueFrom is the value as a reference
                                  to a resource.
                                properties:
                                  secretKeyRef:
                                    description: SecretKeyRef refers to the value
                                      of a specific key in a Secret. You must provide
                                      `name` and `namespace` of the Secret, as well
                                      as the name of the `key`.
                                    properties:
                                      key:
                                        description: Key defines the name of the attribute
                                          of the Secret holding the referenced value.
                                        minLength: 1
                                        type: string
                                      name:
                                        description: Name of the Secret containing
                                          the referenced value.
                                        minLength: 1
                                        type: string
                                      namespace:
                                        description: Namespace containing the Secret
                                          with the referenced value.
                                        minLength: 1
                                        type: string
                                    required:
                                    - key
                                    - name
                                    - namespace
                                    type: object
                                required:
                                - secretKeyRef
                                type: object
                            type: object
                            x-kubernetes-validations:
                            - message: Exactly one of 'value' or 'valueFrom' must
                                be set
                              rule: has(self.value) != has(self.valueFrom)
                          insecure:
                            description: Insecure defines whether to send requests
                              using plaintext instead of TLS.
                            type: boolean
                          insecureSkipVerify:
                            description: InsecureSkipVerify defines whether to skip
                              server certificate verification when using TLS.
                            type: boolean
                          key:
                            description: Defines the client key to use when using
                              TLS. The key must be provided in PEM format.
                            properties:
                              value:
                                description: Value as plain text.
                                type: string
                              valueFrom:
                                description: ValueFrom is the value as a reference
                                  to a resource.
                                properties:
                                  secretKeyRef:
                                    description: SecretKeyRef refers to the value
                                      of a specific key in a Secret. You must provide
                                      `name` and `namespace` of the Secret, as well
                                      as the name of the `key`.
                                    properties:
                                      key:
                                        description: Key defines the name of the attribute
                                          of the Secret holding the referenced value.
                                        minLength: 1
                                        type: string
                                      name:
                                        description: Name of the Secret containing
                                          the referenced value.
                                        minLength: 1
                                        type: string
                                      namespace:
                                        description: Namespace containing the Secret
                                          with the referenced value.
                                        minLength: 1
                                        type: string
                                    required:
                                    - key
                                    - name
                                    - namespace
                                    type: object
                                required:
                                - secretKeyRef
                                type: object
                            type: object
                            x-kubernetes-validations:
                            - message: Exactly one of 'value' or 'valueFrom' must
                                be set
                              rule: has(self.value) != has(self.valueFrom)
                        type: object
                        x-kubernetes-validations:
                        - message: Can define either both 'cert' and 'key', or neither
                          rule: has(self.cert) == has(self.key)
                    required:
                    - endpoint
                    type: object
                    x-kubernetes-validations:
                    - message: Path is only available with HTTP protocol
                      rule: '(has(self.path) && size(self.path) > 0) ? self.protocol
                        == ''http'' : true'
                    - message: OAuth2 authentication requires TLS when using gRPC
                        protocol
                      rule: '(has(self.authentication) && has(self.authentication.oauth2)
                        && self.protocol == ''grpc'' && has(self.tls)) ? !(has(self.tls.insecure)
                        && self.tls.insecure == true) : true'
                required:
                - otlp
                type: object
              transform:
                description: Transforms specify a list of transformations to apply
                  to telemetry data.
                items:
                  description: TransformSpec defines a transformation to apply to
                    telemetry data.
                  properties:
                    conditions:
                      description: Conditions specify a list of multiple where clauses,
                        which will be processed as global conditions for the accompanying
                        set of statements. The conditions are ORed together, which
                        means only one condition needs to evaluate to true in order
                        for the statements (including their individual where clauses)
                        to be executed.
                      items:
                        type: string
                      type: array
                    statements:
                      description: Statements specify a list of OTTL statements to
                        perform the transformation.
                      items:
                        type: string
                      type: array
                  type: object
                type: array
            required:
            - output
            type: object
          status:
            description: Status shows the observed state of the TracePipeline
            properties:
              conditions:
                description: An array of conditions describing the status of the pipeline.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
