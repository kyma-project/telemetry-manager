name: Telemetry Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to release (e.g., 1.2.3)'
        required: true
        type: string
      occ_image:
        description: 'OCC Image Version (e.g., 0.1.0-0.2.0)'
        required: true
        type: string

run-name: "Release Telemetry version ${{ github.event.inputs.version }} with OCC image ${{ github.event.inputs.occ_image }}"

permissions:
  contents: write
  issues: read

jobs:
  validate-and-prepare-branch:
    runs-on: ubuntu-latest
    outputs:
      release-branch: ${{ steps.determine-branch.outputs.release-branch }}
      is-patch: ${{ steps.determine-branch.outputs.is-patch }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check if milestone exists and is closed
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VERSION: ${{ github.event.inputs.version }}
        run: |
          echo "Checking milestone for version ${VERSION}..."
          MILESTONE=$(gh api "repos/${{ github.repository }}/milestones?state=all" --jq ".[] | select(.title == \"${VERSION}\")")

          if [ -z "$MILESTONE" ]; then
            echo "Milestone ${VERSION} does not exist. Please create it first."
            exit 1
          fi

          STATE=$(echo "$MILESTONE" | jq -r '.state')
          if [ "$STATE" != "closed" ]; then
            echo "Milestone ${VERSION} is still open. Please close the milestone before releasing."
            exit 1
          fi

          OPEN_ISSUES=$(echo "$MILESTONE" | jq -r '.open_issues')
          if [ "$OPEN_ISSUES" -gt 0 ]; then
            echo "Milestone ${VERSION} has ${OPEN_ISSUES} open issue(s). Please close all issues before releasing."
            exit 1
          fi

          echo "Milestone ${VERSION} exists, is closed, and has no open issues."

      - name: Check if release already exists
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VERSION: ${{ github.event.inputs.version }}
        run: |
          echo "Checking if release ${VERSION} already exists..."
          if gh release view "${VERSION}" --repo "${{ github.repository }}" > /dev/null 2>&1; then
            echo "Release ${VERSION} already exists. Please choose a different version."
            exit 1
          fi
          echo "Release ${VERSION} does not exist yet."

      - name: Determine release branch and type
        id: determine-branch
        env:
          VERSION: ${{ github.event.inputs.version }}
        run: |
          # Extract major.minor from version
          RELEASE_BRANCH="release-$(echo ${VERSION} | cut -d. -f1-2)"
          echo "release-branch=${RELEASE_BRANCH}" >> $GITHUB_OUTPUT

          # Check if patch release (branch already exists)
          git fetch origin "${RELEASE_BRANCH}" 2>/dev/null || true
          if git rev-parse --verify "origin/${RELEASE_BRANCH}" > /dev/null 2>&1; then
            echo "is-patch=true" >> $GITHUB_OUTPUT
            echo "Patch release detected. Branch ${RELEASE_BRANCH} exists."
          else
            echo "is-patch=false" >> $GITHUB_OUTPUT
            echo "Minor/Major release detected. Branch ${RELEASE_BRANCH} will be created."
          fi

      - name: Create release branch (if minor/major release)
        if: steps.determine-branch.outputs.is-patch == 'false'
        env:
          RELEASE_BRANCH: ${{ steps.determine-branch.outputs.release-branch }}
        run: |
          echo "Creating release branch ${RELEASE_BRANCH} from main..."
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -b "${RELEASE_BRANCH}"
          git push origin "${RELEASE_BRANCH}"
          echo "Created branch ${RELEASE_BRANCH}"

  prepare-release:
    needs: validate-and-prepare-branch
    runs-on: ubuntu-latest
    outputs:
      release-tag: ${{ github.event.inputs.version }}
    steps:
      - name: Checkout release branch
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.validate-and-prepare-branch.outputs.release-branch }}
          fetch-depth: 0

      - name: Setup Golang
        uses: "./.github/template/setup-golang"

      - name: Bump version in files
        env:
          VERSION: ${{ github.event.inputs.version }}
          OCC_IMAGE: ${{ github.event.inputs.occ_image }}
          RELEASE_BRANCH: ${{ needs.validate-and-prepare-branch.outputs.release-branch }}
        run: |
          echo "Working on release branch ${RELEASE_BRANCH}..."                                                                                                                                                                                                                                         
          
          # Ensure we're on the correct branch                                                                                                                                                                                                                                                          
          git checkout "${RELEASE_BRANCH}"
          git pull origin "${RELEASE_BRANCH}"
          
          echo "Bumping version to ${VERSION}..."

          # Update .env file with release version and OCC image
          if [ -f .env ]; then
            # Update ENV_HELM_RELEASE_VERSION to release version
            sed -i.bak "s/ENV_HELM_RELEASE_VERSION=.*/ENV_HELM_RELEASE_VERSION=${VERSION}/" .env && rm .env.bak
            echo "Updated ENV_HELM_RELEASE_VERSION in .env"

            # Update ENV_MANAGER_IMAGE tag to release version
            sed -i.bak "s|ENV_MANAGER_IMAGE=\(.*\):.*|ENV_MANAGER_IMAGE=\1:${VERSION}|" .env && rm .env.bak
            echo "Updated ENV_MANAGER_IMAGE tag in .env"

            # Update ENV_OTEL_COLLECTOR_IMAGE tag to OCC image version
            sed -i.bak "s|ENV_OTEL_COLLECTOR_IMAGE=\(.*\):.*|ENV_OTEL_COLLECTOR_IMAGE=\1:${OCC_IMAGE}\"|" .env && rm .env.bak
            echo "Updated ENV_OTEL_COLLECTOR_IMAGE tag in .env"

            # Run make generate to update generated files
            make generate
            echo "Ran make generate"
          fi

      - name: Commit version bump
        env:
          VERSION: ${{ github.event.inputs.version }}
          RELEASE_BRANCH: ${{ needs.validate-and-prepare-branch.outputs.release-branch }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add .
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Bump version to ${VERSION}"
            git push origin "${RELEASE_BRANCH}"
            echo "Committed and pushed version bump"
          fi

      - name: Create and push release tag
        env:
          VERSION: ${{ github.event.inputs.version }}
          RELEASE_BRANCH: ${{ needs.validate-and-prepare-branch.outputs.release-branch }}
        run: |
          echo "Creating release tag ${VERSION} on release branch ${RELEASE_BRANCH}..."

          # Ensure we have the latest changes from the release branch
          git fetch origin "${RELEASE_BRANCH}"
          git checkout "${RELEASE_BRANCH}"
          git pull origin "${RELEASE_BRANCH}"

          # Create and push the tag from the release branch
          git tag -a "${VERSION}" -m "Release ${VERSION}"
          git push origin "${VERSION}"

          echo "Created and pushed tag ${VERSION} from ${RELEASE_BRANCH}"
          echo "Tag ${VERSION} pushed. This will trigger tag-release.yml workflow."

  build-image:
    permissions:
      id-token: write # Required for requesting the JWT token
      contents: read
    needs: prepare-release
    uses: kyma-project/test-infra/.github/workflows/image-builder.yml@main # Usage: kyma-project/test-infra/.github/workflows/image-builder.yml@main
    with:
      name: telemetry-manager
      dockerfile: Dockerfile
      context: .
      tags: ${{ needs.prepare-release.outputs.release-tag }}
      platforms: "linux/amd64,linux/arm64"

  unit-tests:
    runs-on: ubuntu-latest
    needs: validate-and-prepare-branch
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: ${{ needs.validate-and-prepare-branch.outputs.release-branch }}
          fetch-depth: 0

      - name: Setup Golang
        uses: "./.github/template/setup-golang"

      - name: Run tests
        run: make test

  run-pr-integration:
    needs:
      - build-image
      - prepare-release
    uses: ./.github/workflows/pr-integration.yml
    with:
      image_tag: ${{ needs.prepare-release.outputs.release-tag }}
      ref: ${{ needs.prepare-release.outputs.release-tag }}
    secrets: inherit

  run-gardener-integration:
    needs:
      - build-image
      - prepare-release
    uses: ./.github/workflows/gardener-integration.yml
    with:
      image_tag: ${{ needs.prepare-release.outputs.release-tag }}
      ref: ${{ needs.prepare-release.outputs.release-tag }}
    secrets: inherit

  upload-release-report:
    needs:
      - run-pr-integration
      - run-gardener-integration
      - unit-tests
    uses: kyma/compliancy/.github/workflows/upload-release-report.yml@main
    with:
      release_version: ${{ github.event.inputs.version }}
    secrets:
      RELEASE_LOG_UPLOADER_SA: ${{ secrets.RELEASE_LOG_UPLOADER_SA }}

  create-release:
    needs:
      - prepare-release
      - upload-release-report
    permissions:
      contents: write
    runs-on: ubuntu-latest
    env:
      KYMA_TELEMETRY_MANAGER_REPO: ${{ github.repository }}
    steps:
      - name: Checkout release tag
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare-release.outputs.release-tag }}
          fetch-depth: 0

      - name: Install tools
        shell: bash
        run: make install-tools

      - name: Release
        run: hack/release.sh ${{ needs.prepare-release.outputs.release-tag }}
        env:
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
          GORELEASER_CURRENT_TAG: "${{ needs.prepare-release.outputs.release-tag }}"

      - name: Package Helm chart
        run: |
          OUTPUT=$(helm package ./helm -d ./)
          FILENAME=$(echo $OUTPUT | grep -o '[^ ]*.tgz')
          echo "HELM_CHART=$FILENAME" >> $GITHUB_ENV

      - name: Upload Helm chart to release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          [[ ! -e ${HELM_CHART} ]] && echo "::error ::Packaged Helm chart does not exist" && exit 1
          gh release upload ${{ needs.prepare-release.outputs.release-tag }} --clobber ${HELM_CHART}

      - name: Commit and push Helm chart metadata
        run: |
          git config --global user.name "${GITHUB_ACTOR}"
          git config --global user.email "${GITHUB_ACTOR}@users.noreply.github.com"
          git checkout gh-pages
          helm repo index --url https://github.com/${{ github.repository }}/releases/download/${{ needs.prepare-release.outputs.release-tag }} --merge ./index.yaml ./
          git add index.yaml
          git commit -m "Update Helm chart metadata for ${{ needs.prepare-release.outputs.release-tag }}"
          git push origin gh-pages