name: Docker login
description: Logs into a Docker registry using the provided credentials.

runs:
  using: "composite"

steps:
  - name: Login to Docker Hub
    uses: docker/login-action@v3
    with:
      username: ${{ secrets.DOCKER_USERNAME }}
      password: ${{ secrets.DOCKER_PASSWORD }}