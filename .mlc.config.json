{
  "replacementPatterns": [
    {
      "_comment": "a replacement rule for all the in-repository references",
      "pattern": "^/",
      "replacement": "{{BASEURL}}/"
    }
  ],
  "ignorePatterns": [ 
    {
      "pattern": "^https://raw\\.githubusercontent\\.com/kyma-project/telemetry-manager/main/docs/user/integration/sap-cloud-logging/.+\\.json"
    },
}
